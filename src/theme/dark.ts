// eslint-disable-next-line import/no-extraneous-dependencies
import { ThemeDefinition } from 'vuetify';

const darkTheme: ThemeDefinition = {
  dark: true,
  colors: {
    background: '#161616',
    primary: '#FF1616',
    'primary-lighter': '#FF5A5A',
  },
};

export default darkTheme;
